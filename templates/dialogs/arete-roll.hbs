{{!-- Il tiro di Areté. DialogV2 mette questo contenuto dentro un suo <form>:
     un form dentro un form il browser lo butta via, quindi qui c'è un <div>.
     Con `step` (l'Areté semplificata, 8/9) la finestra mostra un passo solo:
     1 cosa vuoi, 2 come lo fai, 3 il conto; i passi già fatti viaggiano come
     campi nascosti (`carry`), così il conto e il tiro leggono tutto. --}}
<div class="wod5e-mage-arete-body">
    {{#if simple}}
        <ol class="wod5e-mage-arete-steps" aria-label="{{localize 'WOD5E_MAGE.Arete.Simple'}}">
            <li class="{{#if show.goal}}active{{/if}}"><b>1</b> {{localize "WOD5E_MAGE.Arete.Steps.1"}}</li>
            <li class="{{#if show.traits}}active{{/if}}"><b>2</b> {{localize "WOD5E_MAGE.Arete.Steps.2"}}</li>
            <li class="{{#if show.conto}}active{{/if}}"><b>3</b> {{localize "WOD5E_MAGE.Arete.Steps.3"}}</li>
        </ol>
    {{/if}}
    <div class="wod5e-mage-arete-layout{{#if simple}} wod5e-mage-arete-simple{{/if}}" data-arete="{{arete.value}}">
        {{!-- Colonna sinistra: nome, Obiettivo, Effetto, Sfere e Ambiti a pallini. Per ogni
             Sfera scegli il livello che usi, per ogni Ambito il livello da 1 a 7. --}}
        {{!-- Una griglia sola: simbolo | nome | pallini. Le file sono trasparenti
             (display: contents), così i pallini di tutte le righe partono dalla
             stessa verticale, subito dopo il nome più lungo, a sinistra. --}}
        <div class="wod5e-mage-arete-dots-column" role="group" aria-label="{{localize 'WOD5E_MAGE.Arete.Spheres'}}">
            {{#if saveMode}}
                {{!-- Il Grimorio del personaggio: il nome dell'incantesimo, in cima. --}}
                <label class="wod5e-mage-arete-goal wod5e-mage-arete-spell">
                    <span class="wod5e-mage-arete-row-label">{{localize "WOD5E_MAGE.Incantesimi.Name"}}</span>
                    <input type="text" name="spellName" id="wod5e-mage-arete-spell-name" placeholder="{{localize 'WOD5E_MAGE.Incantesimi.NameHint'}}" aria-label="{{localize 'WOD5E_MAGE.Incantesimi.Name'}}">
                </label>
            {{/if}}
            {{!-- L'Effetto: fisico, mentale o variabile. Dice dove va il Contraccolpo. --}}
            {{#if show.effect}}
            <div class="wod5e-mage-arete-goal wod5e-mage-arete-effect-kind">
                <span class="wod5e-mage-arete-row-label">{{localize "WOD5E_MAGE.Arete.EffectKind"}}</span>
                <select name="effectKind" id="wod5e-mage-arete-effect-kind" aria-label="{{localize 'WOD5E_MAGE.Arete.EffectKind'}}">
                    <option value="">{{localize "WOD5E_MAGE.Arete.EffectKinds.none"}}</option>
                    <option value="physical">{{localize "WOD5E_MAGE.Arete.EffectKinds.physical"}}</option>
                    <option value="mental">{{localize "WOD5E_MAGE.Arete.EffectKinds.mental"}}</option>
                    <option value="variable">{{localize "WOD5E_MAGE.Arete.EffectKinds.variable"}}</option>
                </select>
            </div>
            {{else if carry.effect}}
                <input type="hidden" name="effectKind" id="wod5e-mage-arete-effect-kind" value="{{carry.effectKind}}">
            {{/if}}
            {{#if show.spheres}}
            <span class="wod5e-mage-arete-row-label wod5e-mage-arete-dots-head">{{localize "WOD5E_MAGE.Arete.Spheres"}}</span>
            {{#each spheres as |sphere|}}
                <div class="wod5e-mage-arete-dotrow" data-role="dotRow" data-kind="sphere" data-id="{{sphere.id}}" data-specialty="{{sphere.specialtyScope}}">
                    <img class="wod5e-mage-arete-row-icon" src="{{sphere.icon}}" alt="">
                    <span class="wod5e-mage-arete-dot-label">{{localize sphere.label}}{{#if sphere.specialtyScope}} <small class="wod5e-mage-arete-specialty" title="{{localize 'WOD5E_MAGE.Arete.SpecialtyHint'}}">◆ {{localize sphere.specialtyLabel}}</small>{{/if}}</span>
                    <input type="hidden" name="sphere-{{sphere.id}}" value="0">
                    <span class="wod5e-mage-arete-sphere-dots" role="group" aria-label="{{localize sphere.label}}">
                        {{#each sphere.steps as |step|}}<button type="button" class="wod5e-mage-arete-sphere-dot" data-level="{{step.value}}" aria-label="{{step.value}}" title="{{step.value}}"></button>{{/each}}
                    </span>
                    {{!-- Il nome del livello scelto (Percepire … Rivoluzionare), a destra. --}}
                    <span class="wod5e-mage-arete-dot-reading" data-role="dotReading" aria-live="polite"></span>
                </div>
            {{else}}
                <p class="wod5e-mage-magick-type-hint">{{localize "WOD5E_MAGE.Arete.NoSpheres"}}</p>
            {{/each}}
            {{else}}
                {{!-- I passi dopo il primo portano le Sfere scelte come file nascoste:
                     il conto le legge come se fossero a pallini. --}}
                {{#each carry.spheres as |row|}}
                    <span data-role="dotRow" data-kind="sphere" data-id="{{row.id}}" data-specialty="{{row.specialty}}" hidden><input type="hidden" name="sphere-{{row.id}}" value="{{row.level}}"></span>
                {{/each}}
            {{/if}}

            {{!-- L'Obiettivo, sotto le Sfere (6/9): cosa vuoi ottenere, scritto a mano o scelto dal
                 Grimorio (il libro), che accende anche le Sfere coinvolte. --}}
            {{!-- L'Obiettivo è un riquadro (6/9): c'è spazio per scrivere, e la
                 colonna sinistra pareggia la destra. Nel primo passo della
                 semplificata sta in cima (CSS: order). --}}
            {{#if show.goal}}
            <div class="wod5e-mage-arete-goal wod5e-mage-arete-goal-box">
                <span class="wod5e-mage-arete-row-label">{{localize "WOD5E_MAGE.Arete.Goal"}}</span>
                <textarea name="goal" id="wod5e-mage-arete-goal" rows="4" placeholder="{{localize 'WOD5E_MAGE.Arete.GoalHint'}}" aria-label="{{localize 'WOD5E_MAGE.Arete.Goal'}}"></textarea>
                <button type="button" class="wod5e-mage-archivio-open wod5e-mage-arete-grimorio" data-role="grimorioOpen" title="{{localize 'WOD5E_MAGE.Grimorio.Open'}}" aria-label="{{localize 'WOD5E_MAGE.Grimorio.Open'}}"><i class="fa-solid fa-book-open" aria-hidden="true"></i></button>
            </div>
            {{else}}
                <input type="hidden" name="goal" value="{{carry.goal}}">
                {{#if carry.goal}}
                    {{!-- L'Obiettivo scelto al primo passo, in lettura. --}}
                    <p class="wod5e-mage-arete-carry-goal"><b>{{localize "WOD5E_MAGE.Arete.Goal"}}</b> {{carry.goal}}</p>
                {{/if}}
            {{/if}}
            {{#if show.scopes}}
            {{!-- L'icona della tavola sta a sinistra della parola. --}}
            <span class="wod5e-mage-arete-row-label wod5e-mage-arete-dots-head wod5e-mage-arete-scopes-head">
                <button type="button" class="wod5e-mage-arete-scope-table" data-role="scopeTableOpen" title="{{localize 'WOD5E_MAGE.Arete.ScopeTable'}}" aria-label="{{localize 'WOD5E_MAGE.Arete.ScopeTable'}}"><i class="fa-solid fa-table-list"></i></button>
                {{localize "WOD5E_MAGE.Scopes.Label"}}
            </span>
            {{#each scopes as |scope|}}
                <div class="wod5e-mage-arete-dotrow" data-role="dotRow" data-kind="scope" data-id="{{scope.id}}">
                    <i class="wod5e-mage-arete-row-icon {{scope.faIcon}}" aria-hidden="true"></i>
                    <span class="wod5e-mage-arete-dot-label">{{localize scope.label}}</span>
                    <input type="hidden" name="scope-{{scope.id}}" value="0">
                    <span class="wod5e-mage-arete-sphere-dots" role="group" aria-label="{{localize scope.label}}">
                        {{#each scope.steps as |step|}}<button type="button" class="wod5e-mage-arete-sphere-dot" data-level="{{step.value}}" aria-label="{{step.value}}" title="{{step.value}}"></button>{{/each}}
                    </span>
                    {{!-- La voce della tavola per il livello scelto («Città»), a destra dei pallini (9/9). --}}
                    <span class="wod5e-mage-arete-dot-reading" data-role="dotReading" aria-live="polite"></span>
                </div>
            {{/each}}
            {{else}}
                {{#each carry.scopes as |row|}}
                    <span data-role="dotRow" data-kind="scope" data-id="{{row.id}}" hidden><input type="hidden" name="scope-{{row.id}}" value="{{row.level}}"></span>
                {{/each}}
            {{/if}}

            {{!-- Il conto, aggiornato a ogni scelta: riserva contro soglia. Al primo
                 passo della semplificata la riserva non c'è ancora: solo la soglia. --}}
            <p class="wod5e-mage-arete-difficulty">
                <span{{#unless show.pool}} hidden{{/unless}}>{{localize "WOD5E_MAGE.Arete.Pool"}}: <strong data-role="pool">0</strong> · </span>{{localize "WOD5E_MAGE.Arete.Threshold"}}: <strong data-role="threshold">0</strong>
                <span class="wod5e-mage-arete-autosuccess" data-role="autoSuccesses"></span>
            </p>
            {{#if show.conto}}
            <p class="wod5e-mage-arete-hint">{{localize "WOD5E_MAGE.Arete.ThresholdShort"}}</p>
            <p class="wod5e-mage-arete-auto hidden" data-role="autoVictory">{{localize "WOD5E_MAGE.Arete.AutoVictory"}}</p>
            {{/if}}
        </div>

        {{!-- Colonna destra: la riserva, la narrativa, il premio, la Tipologia. --}}
        {{#if show.side}}
        <div class="wod5e-mage-arete-side">
            {{!-- La riserva (ramo A): Attributo, Abilità, Abilità. Ne basta uno; chi
                 ne sceglie di più somma i dadi. L'Areté non tira: è il premio. --}}
            {{#if show.traits}}
            <label class="wod5e-mage-arete-trait">
                <span class="wod5e-mage-arete-row-label">{{localize "WOD5E_MAGE.RollSelection.Attribute"}}</span>
                <select id="wod5e-mage-arete-attribute" name="attributeTrait">
                    <option value="" data-value="0">{{localize "WOD5E_MAGE.RollSelection.None"}}</option>
                    {{#each attributes as |trait|}}
                        <option value="{{trait.key}}" data-value="{{trait.value}}">{{trait.label}} ({{trait.value}})</option>
                    {{/each}}
                </select>
            </label>
            <label class="wod5e-mage-arete-trait">
                <span class="wod5e-mage-arete-row-label">{{localize "WOD5E_MAGE.RollSelection.Ability"}}</span>
                <select id="wod5e-mage-arete-primary" name="primaryTrait">
                    <option value="" data-value="0">{{localize "WOD5E_MAGE.RollSelection.None"}}</option>
                    {{#each skills as |trait|}}
                        <option value="{{trait.key}}" data-value="{{trait.value}}">{{trait.label}} ({{trait.value}})</option>
                    {{/each}}
                </select>
            </label>
            <label class="wod5e-mage-arete-trait">
                <span class="wod5e-mage-arete-row-label">{{localize "WOD5E_MAGE.RollSelection.Ability"}}</span>
                <select id="wod5e-mage-arete-secondary" name="secondaryTrait">
                    <option value="" data-value="0">{{localize "WOD5E_MAGE.RollSelection.None"}}</option>
                    {{#each skills as |trait|}}
                        <option value="{{trait.key}}" data-value="{{trait.value}}">{{trait.label}} ({{trait.value}})</option>
                    {{/each}}
                </select>
            </label>
            {{else}}
                {{!-- Al terzo passo la riserva è già scelta: i tratti viaggiano nascosti,
                     col loro valore, e il conto li legge da lì. --}}
                {{#each carry.traits as |trait|}}
                    <input type="hidden" name="{{trait.field}}" id="{{trait.id}}" value="{{trait.key}}" data-value="{{trait.value}}">
                {{/each}}
                {{#if carry.traitLabels}}
                    <p class="wod5e-mage-arete-carry-goal"><b>{{localize "WOD5E_MAGE.Arete.Pool"}}</b> {{carry.traitLabels}}</p>
                {{/if}}
            {{/if}}

            {{#if saveMode}}
                {{!-- La narrativa: come si lancia, sulla base delle Abilità scelte. --}}
                <label class="wod5e-mage-arete-narrative">
                    <span class="wod5e-mage-arete-row-label">{{localize "WOD5E_MAGE.Incantesimi.Narrative"}}</span>
                    <textarea name="narrative" id="wod5e-mage-arete-narrative" rows="5" placeholder="{{localize 'WOD5E_MAGE.Incantesimi.NarrativeHint'}}" aria-label="{{localize 'WOD5E_MAGE.Incantesimi.Narrative'}}"></textarea>
                </label>
            {{/if}}

            {{#if show.conto}}
            {{!-- Il premio dell'Areté: dadi in più pari all'Areté quando la descrizione
                 lo merita, dentro il tetto +3 insieme ai Bonus. L'Ibrida non lo prende. --}}
            {{!-- Il nome per intero a sinistra, la casella (col suo +N) a destra. --}}
            <div class="wod5e-mage-arete-prize" title="{{localize 'WOD5E_MAGE.Arete.PrizeHint' arete=arete.value}}">
                <span class="wod5e-mage-arete-row-label wod5e-mage-arete-row-label-full">{{localize "WOD5E_MAGE.Arete.Prize"}}</span>
                {{#if prize.allowed}}
                    <label class="wod5e-mage-arete-toggle">
                        <span class="wod5e-mage-arete-toggle-value">+{{prize.dice}}</span>
                        <input type="checkbox" name="prize" data-value="{{prize.dice}}" aria-label="{{localize 'WOD5E_MAGE.Arete.Prize'}}">
                    </label>
                {{else}}
                    <em>{{localize "WOD5E_MAGE.Arete.PrizeHybrid"}}</em>
                {{/if}}
            </div>

            {{#unless saveMode}}
            {{!-- L'Armonia: i dadi che gli altri Maghi ti danno, contati al tavolo. --}}
            <div class="wod5e-mage-arete-harmony-row" title="{{localize 'WOD5E_MAGE.Arete.HarmonyHint'}}">
                <span class="wod5e-mage-arete-row-label wod5e-mage-arete-row-label-full">{{localize "WOD5E_MAGE.Arete.Harmony"}} <small>{{localize "WOD5E_MAGE.Arete.HarmonyShort"}}</small></span>
                <input type="number" name="harmony" id="wod5e-mage-arete-harmony" class="wod5e-mage-arete-harmony" min="0" max="9" step="1" value="0" data-value="0" aria-label="{{localize 'WOD5E_MAGE.Arete.Harmony'}}">
            </div>

            {{!-- La Quintessenza: ogni punto speso è un successo automatico e scende
                 dalla Ruota; se l'effetto fa danni, sono aggravati. --}}
            <div class="wod5e-mage-arete-harmony-row wod5e-mage-arete-quintessence-row" title="{{localize 'WOD5E_MAGE.Arete.QuintessenceHint'}}">
                <span class="wod5e-mage-arete-row-label wod5e-mage-arete-row-label-full">{{localize "WOD5E_MAGE.Arete.Quintessence"}} <small>{{localize "WOD5E_MAGE.Arete.QuintessenceShort" available=quintessence}}</small></span>
                <input type="number" name="quintessence" id="wod5e-mage-arete-quintessence" class="wod5e-mage-arete-harmony" min="0" max="{{quintessence}}" step="1" value="0" aria-label="{{localize 'WOD5E_MAGE.Arete.Quintessence'}}" {{#unless quintessence}}disabled{{/unless}}>
            </div>

            {{!-- La Convinzione rispettata (9/9): spuntata e scelta fra quelle in scheda,
                 a tiro fatto un punto di Quintessenza risale sulla Ruota, una volta per scena. --}}
            <div class="wod5e-mage-arete-conviction" title="{{localize 'WOD5E_MAGE.Arete.ConvictionHint'}}">
                {{#if conviction.options.length}}
                    <div data-role="convictionFresh" {{#if conviction.used}}hidden{{/if}}>
                        <label class="wod5e-mage-arete-conviction-toggle">
                            <input type="checkbox" name="conviction" id="wod5e-mage-arete-conviction">
                            <span>{{localize "WOD5E_MAGE.Arete.Conviction"}} <small>{{localize "WOD5E_MAGE.Arete.ConvictionShort"}}</small></span>
                        </label>
                        <select name="convictionId" id="wod5e-mage-arete-conviction-id" class="hidden" aria-label="{{localize 'WOD5E_MAGE.Arete.ConvictionPick'}}">
                            {{#each conviction.options as |option|}}
                                <option value="{{option.id}}">{{option.label}}</option>
                            {{/each}}
                        </select>
                    </div>
                    <div data-role="convictionUsed" class="wod5e-mage-arete-conviction-used" {{#unless conviction.used}}hidden{{/unless}}>
                        <span class="wod5e-mage-arete-row-label">{{localize "WOD5E_MAGE.Arete.Conviction"}}</span>
                        <em>{{localize "WOD5E_MAGE.Arete.ConvictionUsed"}}</em>
                        <button type="button" data-role="convictionReset" title="{{localize 'WOD5E_MAGE.Arete.ConvictionNewSceneHint'}}"><i class="fa-solid fa-clapperboard" aria-hidden="true"></i> {{localize "WOD5E_MAGE.Arete.ConvictionNewScene"}}</button>
                    </div>
                {{else}}
                    <span class="wod5e-mage-arete-row-label">{{localize "WOD5E_MAGE.Arete.Conviction"}}</span>
                    <em>{{localize "WOD5E_MAGE.Arete.ConvictionNone"}}</em>
                {{/if}}
            </div>
            {{/unless}}
            {{/if}}

            {{#if show.types}}
            {{#if show.conto}}<hr>{{/if}}

            {{!-- La Tipologia: una scelta sola. --}}
            <div class="wod5e-mage-arete-types" role="group" aria-label="{{localize 'WOD5E_MAGE.Arete.MagickType'}}">
                <span class="wod5e-mage-arete-row-label">{{localize "WOD5E_MAGE.Arete.MagickType"}}</span>
                <div class="wod5e-mage-arete-type-grid">
                    <label class="wod5e-mage-arete-type"><input type="checkbox" class="wod5e-mage-magick-type" name="coincidental"> <span>{{localize "WOD5E_MAGE.Arete.Coincidental"}}</span></label>
                    <em>{{localize "WOD5E_MAGE.Arete.CoincidentalHint"}}</em>
                    <label class="wod5e-mage-arete-type"><input type="checkbox" class="wod5e-mage-magick-type" name="vulgar"> <span>{{localize "WOD5E_MAGE.Arete.Vulgar"}}</span></label>
                    <em>{{localize "WOD5E_MAGE.Arete.VulgarHint"}}</em>
                    <label class="wod5e-mage-arete-type"><input type="checkbox" class="wod5e-mage-magick-type" name="witnesses"> <span>{{localize "WOD5E_MAGE.Arete.VulgarWithWitnesses"}}</span></label>
                    <em>{{localize "WOD5E_MAGE.Arete.WitnessesHint"}}</em>
                </div>
            </div>
            {{else}}
                {{!-- La Tipologia scelta al secondo passo, nascosta: una casella spuntata vale «on». --}}
                {{#if carry.coincidental}}<input type="hidden" name="coincidental" value="on">{{/if}}
                {{#if carry.vulgar}}<input type="hidden" name="vulgar" value="on">{{/if}}
                {{#if carry.witnesses}}<input type="hidden" name="witnesses" value="on">{{/if}}
            {{/if}}

            {{#if show.conto}}
            <hr>

            {{!-- L'Effetto Mantenuto: se spuntato compare il nome, e a tiro risolto
                 l'effetto finisce fra le Magick in Atto. --}}
            <div class="wod5e-mage-arete-maintained">
                <label class="wod5e-mage-arete-maintained-toggle">
                    <input type="checkbox" name="maintained" id="wod5e-mage-arete-maintained">
                    {{localize "WOD5E_MAGE.Arete.Maintained"}}
                </label>
                <input type="text" name="maintainedName" id="wod5e-mage-arete-maintained-name" class="hidden"
                       placeholder="{{localize 'WOD5E_MAGE.Arete.MaintainedName'}}"
                       aria-label="{{localize 'WOD5E_MAGE.Arete.MaintainedName'}}">
            </div>
            {{/if}}
        </div>
        {{/if}}
    </div>
</div>
